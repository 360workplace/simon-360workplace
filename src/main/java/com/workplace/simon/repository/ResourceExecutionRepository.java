package com.workplace.simon.repository;

import com.workplace.simon.model.ResourceExecution;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResourceExecutionRepository extends JpaRepository<ResourceExecution, Long> {
}
